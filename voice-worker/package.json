{
  "name": "novelverse-voice-worker",
  "version": "1.0.0",
  "description": "Production local voice worker for NovelVerse TTS previews, synthesis, and transformations.",
  "type": "module",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "build": "node utils/build-check.js",
    "lint": "node utils/lint-check.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "file:vendor/dotenv",
    "express": "file:vendor/express"
  }
}
