# Story Memory

Story context records store compact memory, not full chapter text: important recent events, active characters, unresolved conflicts, current relationships, current character states, locations and chronology markers.

Contradiction detection emits warnings for dead characters appearing alive, unexplained age regression and duplicate normalized identities. Warnings are review signals and never perform destructive fixes.
